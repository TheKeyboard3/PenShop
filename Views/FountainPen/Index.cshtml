@model IEnumerable<int>

@{
    ViewData["Title"] = "Fountain pens";
}

<h1>Fountain pens</h1>

<p>
    <a asp-controller="Pen" asp-action="Index">Pens</a> |
    <a asp-action="Create">Create New</a>
</p>
<div class="container">
    <div class="row">
        @foreach (var item in Model) {
            @Html.RenderAction("ProductCard", new {id = item});
        }
    </div>
</div>
